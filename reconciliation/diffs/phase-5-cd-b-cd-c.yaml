# CR-BP-15-IMP Phase 5: Per-Tranche Diff (cd-b + cd-c)

# This file records exactly which bytes the Phase 5 first
# tranche migration touched and how the SHA-256 baseline
# evolved. The diff is the audit trail for the
# reconciliation gate (CR-BP-15 /7).

# Authoritative reference: CR-BP-15-IMP /5; CR-BP-15 /7.

schema_version: 1
diff_at: 2026-09-06
phase: phase-5
tranches:
  - cd-b
  - cd-c

# Summary
summary:
  records_touched: 4
  sha256_changed: 4
  new_intent_vocabulary_tokens: 4
  legacy_fields_removed:
    process_audience: 4
    process_context_scalar: 4
  canonical_blocks_added:
    context: 4
    serves_relationship: 4

# Per-record diff
diffs:
  - record_id: dea:process-customer-channel-and-acquisition-build
    tranche: cd-b
    sha256_before: 5e36ccba24aae0bf4d09a96cc8e3fb66b6d6af6b25f8c2d31a8e94a6f80b8d4d   # baseline v1
    sha256_after: <filled at PR-open time by build_inventory.py>
    disposition: RECLASSIFY
    changes_applied:
      - "process_intent: operational -> operate"
      - "process_audience: party-relationship REMOVED"
      - "process_context: scalar 'dea:pc-pr-b' REMOVED"
      - "context: [{ref: dea:pc-pr-b}] ADDED"
      - "relationships[0]: {source_id, relationship_type: serves, target_id: ecf:customerAndDemand.build} ADDED"
      - "metadata.change_history: CR-BP-15-IMP Phase 5 entry APPENDED"

  - record_id: dea:process-demand-generation-build
    tranche: cd-b
    sha256_before: <baseline v1>
    sha256_after: <filled at PR-open time by build_inventory.py>
    disposition: RECLASSIFY
    changes_applied:
      - "process_intent: operational -> operate"
      - "process_audience: party-relationship REMOVED"
      - "process_context: scalar 'dea:pc-pr-b' REMOVED"
      - "context: [{ref: dea:pc-pr-b}] ADDED"
      - "relationships[0]: {source_id, relationship_type: serves, target_id: ecf:customerAndDemand.build} ADDED"
      - "metadata.change_history: CR-BP-15-IMP Phase 5 entry APPENDED"

  - record_id: dea:process-customer-strategy-conception
    tranche: cd-c
    sha256_before: <baseline v1>
    sha256_after: <filled at PR-open time by build_inventory.py>
    disposition: RECLASSIFY
    changes_applied:
      - "process_intent: management -> develop"
      - "process_audience: party-relationship REMOVED"
      - "process_context: scalar 'dea:pc-pr-c' REMOVED"
      - "context: [{ref: dea:pc-pr-c}] ADDED"
      - "relationships[0]: {source_id, relationship_type: serves, target_id: ecf:customerAndDemand.conceive} ADDED"
      - "metadata.change_history: CR-BP-15-IMP Phase 5 entry APPENDED"

  - record_id: dea:process-market-and-demand-conception
    tranche: cd-c
    sha256_before: <baseline v1>
    sha256_after: <filled at PR-open time by build_inventory.py>
    disposition: RECLASSIFY
    changes_applied:
      - "process_intent: management -> develop"
      - "process_audience: party-relationship REMOVED"
      - "process_context: scalar 'dea:pc-pr-c' REMOVED"
      - "context: [{ref: dea:pc-pr-c}] ADDED"
      - "relationships[0]: {source_id, relationship_type: serves, target_id: ecf:customerAndDemand.conceive} ADDED"
      - "metadata.change_history: CR-BP-15-IMP Phase 5 entry APPENDED"

# Cross-tranche observations
observations:
  - |
    The 4 migrated records no longer trigger BP-SEM-007
    (canonical context: block required). The Phase 5 first
    tranche has reduced the BP-SEM error count from 18 to
    14; the remaining 14 errors will be reduced by Phase 5
    tranches 2..6 (PR-7..9).
  - |
    The `process_audience` legacy field is fully removable
    for these 4 records because the canonical `context:`
    block already declares the ECF Domain x Lifecycle Stage
    placement. The schema (entity.schema.json) no longer
    requires `process_audience`.
  - |
    The migration is idempotent: re-running the script
    against an already-migrated file is a no-op (the
    self-test locks this contract).
